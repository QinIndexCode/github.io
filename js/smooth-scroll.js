(function() {
    // SmoothScroll disabled to use native browser scrolling
    // The user requested to reduce damping and delay, so we fallback to native CSS scroll-behavior
    console.log('SmoothScroll disabled for better performance');
})();
